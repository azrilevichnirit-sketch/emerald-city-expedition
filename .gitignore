# Secrets — NEVER commit
keys/
gimini_key/
leonardo_key/
openai/
*.key
*_KEY.txt
access_token.txt

# Claude Code local state
.claude/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# OS / editor crud
~$*
Thumbs.db
.DS_Store

# rembg model cache (downloaded to ~/.u2net usually but sometimes here)
.u2net/

# Local-only staging that shouldn't sync (workflow commits these in CI)
# NOTE: pipeline/staging/shutterstock/ IS committed by the CI workflow as
# output. Don't ignore it.

# Shutterstock pipeline disabled (rembg destroyed scenery shots).
# Re-enable only after post-processing logic is fixed.
pipeline/staging/shutterstock/

# Large media not committed to git (kept locally — tracked via asset_catalog.json)
assets/
master_player/
pipeline/review/
pipeline/staging/
pipeline/bg_keyframes/
pipeline/pose_keyframes/
pipeline/pose_keyframes_dense/
pipeline/_chroma_sample.png
pipeline/style_reference.png
pipeline/test_rope_bridge.png
*.mp4
*.mov
*.avi

# But DO commit small structural images (under 500KB)
!pipeline/builder_html/**/*.png
!agents/**/*.png

# Backup branches
backup/

